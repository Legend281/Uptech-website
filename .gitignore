# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env
.env
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# Claude Code tooling. settings.json is tracked (it records the enabled
# security-guidance plugin, per CLAUDE.md Section 7); the skill and agent
# packages under it are third-party code installed locally, not ours to vendor.
/.claude/skills/
/.claude/agents/
/.claude/scheduled_tasks.lock

# throwaway browser-verification scripts
.verify-tmp*.mjs
.debug-tmp*.mjs

# verification builds (see distDir in next.config.mjs)
/.next-verify/
